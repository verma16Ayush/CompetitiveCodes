#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    cin.ignore(numeric_limits<streamsize>::max(), '\n');
    int temp;
    vector<int> arr;
    arr.reserve(n);
    for (int i = 0; i < n; i++)
    {
        cin>>temp;
        arr.push_back(temp);
    }

    int j = n-1;
    int i;
    for (i = 0; i < j; i++)
    {
        for (j = n -1; ; i++)
        {
            /* code */
        }
        
    }
}