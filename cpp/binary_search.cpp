#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int bin_search(vector<int>::iterator begin, vector<int>::iterator end, vector<int> &a, int query)
{
    vector<int>::iterator mid = (static_cast<int>(begin) + end) / 2;
    while(begin < end)
    {
        if(query < )
    }
}

int main()
{

}